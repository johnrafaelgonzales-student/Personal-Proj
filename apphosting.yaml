# Settings to manage and configure a Firebase App Hosting backend.
# See: https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Sets the maximum number of server instances that can run at once.
  # Increasing this value allows the app to automatically scale up
  # to handle more traffic.
  maxInstances: 1
